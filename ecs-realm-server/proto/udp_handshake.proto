syntax = "proto3";

package mmorpg.proto;

// [SEQUENCE: MVP6-18] Sent by the client over UDP to associate its UDP endpoint with its authenticated TCP session.
message UdpHandshake {
  uint64 player_id = 1;
}